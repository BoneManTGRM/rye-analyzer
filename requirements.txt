# --- Core runtime ---
streamlit>=1.37.0
pandas>=2.2.2
numpy>=1.26.4

# Charts
plotly>=5.18.0
matplotlib>=3.8.0

# File I/O: Excel/Parquet/Feather/JSON
openpyxl>=3.1.2          # .xlsx
xlrd>=2.0.1              # legacy .xls (optional but small)
pyarrow>=15.0.0          # parquet + feather & fast columnar IO

# Optional scientific formats (NetCDF via xarray)
xarray>=2024.1.0         # high-level dataset handling
netcdf4>=1.6.5           # NetCDF backend for xarray (CPython wheels)
# h5netcdf>=1.3.0        # (alternative backend to netcdf4; comment-in if preferred)

# Stats (used if available; code falls back gracefully)
scipy>=1.12.0

# PDF report
fpdf2>=2.7.9

# Nice-to-haves / misc
requests>=2.32.0
pillow>=10.2.0
watchdog>=3.0.0
python-dateutil>=2.9.0
