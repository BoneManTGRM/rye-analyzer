# Core app dependencies
streamlit>=1.37.0
pandas>=2.0.3
numpy>=1.24.4
plotly>=5.18.0
matplotlib>=3.8.0

# PDF generation (modern and Unicode-safe)
fpdf2>=2.7.9

# Date, time, and file I/O
python-dateutil>=2.9.0
openpyxl>=3.1.0
xlrd>=2.0.1
requests>=2.32.0

# Optional but recommended for images and caching
pillow>=10.2.0
watchdog>=3.0.0

# Machine learning / math helpers (optional, but useful if you expand analytics)
scipy>=1.12.0
scikit-learn>=1.4.0

# Notes:
# - fpdf2 replaces old "fpdf" (do NOT include both).
# - Make sure fonts/DejaVuSans.ttf exists for Unicode PDF rendering.
# - Pillow enables logo and image embedding in PDFs.
# - Watchdog improves Streamlit caching on Streamlit Cloud.
# - This configuration is tested with Python 3.11–3.13.
